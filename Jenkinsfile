pipeline {
  agent any
  stages {
    stage("Installations") {
      steps {
        sh '''
          echo "Hello World"
          ifconfig
          echo "Done for the Day"
        '''
      }
    }
  }
}
